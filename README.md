# Banking-Application
This is a program which uses python and SQL for database.

There is only one pyhton file used in this application "main".
There is a database file in the main folder itself named "accounts.data".
The project is also expoted as a standalone exe file.
